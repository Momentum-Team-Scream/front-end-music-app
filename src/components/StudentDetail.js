import React from 'react';
import axios from 'axios';

export const StudentDetail = ({auth, pk}) => {
    

    
    return (
        <div>
            <p>student details load here</p>
        </div>
    );
}

